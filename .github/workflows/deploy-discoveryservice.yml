
name: Deploy DiscoveryService
on:
  push:
    branches: [main]
    paths: ['DiscoveryService/**']
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - uses: docker/build-push-action@v5
        with:
          context: ./DiscoveryService
          platforms: linux/amd64
          push: true
          tags: stargazingsparrow/discoveryservice:latest
      - uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HETZNER_HOST }}
          username: root
          key: ${{ secrets.HETZNER_SSH_KEY }}
          script: |
            docker pull stargazingsparrow/discoveryservice:latest
            docker rm -f discoveryservice
            cd ~/services/discoveryservice && docker-compose up -d
