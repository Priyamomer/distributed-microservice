apiVersion: v1
kind: Service
metadata:
  name: userserviceservice
spec:
  selector:
    app: userservice
  ports:
    - protocol: TCP
      port: 8200
      targetPort: 8200
      nodePort: 30200
  type: NodePort